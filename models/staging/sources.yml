version: 2

sources:
  - name: raw
    schema: raw
    description: >
      Raw tables loaded by the seed step, standing in for tables an
      EL tool would normally land in a "raw" schema.
    tables:
      - name: raw_customers
      - name: raw_orders
