# dbt 2.0 (Fusion). The PyPI package ships dbt-fusion, a compiled binary
# with built-in adapters -- the Python dbt-spark adapter below is not used
# by Fusion itself but is kept so the classic 1.x engine can still be run.
# WARNING: Fusion does not support method=session or Python models on
# Spark, which this project's Python model needs; expect `dbt run` to fail
# on those until Fusion adds support.
dbt-core==2.0.0a5
